---
"@recallnet/docs-governance-preset": patch
---

Improve `populate` repo discovery so generated docs reflect declared workspaces and common implementation roots like `src/`, `internal/`, and `executors/` instead of assuming only `packages/*` layouts.
