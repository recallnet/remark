name: Publish Packages

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  release:
    if: >-
      github.repository == 'recallnet/remark-ai' &&
      !contains(github.event.head_commit.message, 'chore: version packages [skip ci]')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      actions: read
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - name: Upgrade npm for trusted publishing
        run: npm install -g npm@latest

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check for changesets
        id: changesets
        run: |
          output=$(pnpm changeset status 2>&1) && rc=0 || rc=$?
          if [ "$rc" -eq 0 ]; then
            echo "has_changesets=true" >> "$GITHUB_OUTPUT"
          elif echo "$output" | grep -qi "no.*changesets"; then
            echo "has_changesets=false" >> "$GITHUB_OUTPUT"
          else
            echo "::error::changeset status failed unexpectedly:"
            echo "$output"
            exit 1
          fi

      - name: Build packages
        if: steps.changesets.outputs.has_changesets == 'true'
        run: pnpm build

      - name: Version packages
        if: steps.changesets.outputs.has_changesets == 'true'
        run: pnpm changeset version

      - name: Commit and push version changes
        if: steps.changesets.outputs.has_changesets == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: version packages [skip ci]" || echo "No changes to commit"
          git push

      - name: Snapshot existing tags
        if: steps.changesets.outputs.has_changesets == 'true'
        run: git tag --list | sort > /tmp/tags-before-publish.txt

      - name: Publish packages
        if: steps.changesets.outputs.has_changesets == 'true'
        env:
          NPM_CONFIG_PROVENANCE: "true"
        run: pnpm changeset publish

      - name: Create GitHub releases for published tags
        if: steps.changesets.outputs.has_changesets == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git fetch --tags --force
          git tag --list | sort > /tmp/tags-after-publish.txt
          new_tags=$(comm -13 /tmp/tags-before-publish.txt /tmp/tags-after-publish.txt)

          if [ -z "$new_tags" ]; then
            echo "No new tags were created by publish; skipping GitHub Releases."
            exit 0
          fi

          while IFS= read -r tag; do
            [ -n "$tag" ] || continue

            if gh release view "$tag" >/dev/null 2>&1; then
              echo "GitHub Release already exists for $tag; skipping."
              continue
            fi

            gh release create "$tag" \
              --title "$tag" \
              --verify-tag \
              --generate-notes
          done <<EOF
          $new_tags
          EOF
      - name: Open ticket on failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: .github/scripts/ci-failure-ticket.sh "publish-packages" "${{ github.run_id }}"
